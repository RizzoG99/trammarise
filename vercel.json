{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cross-Origin-Embedder-Policy",
          "value": "require-corp"
        },
        {
          "key": "Cross-Origin-Opener-Policy",
          "value": "same-origin"
        }
      ]
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/transcribe.ts": {
      "maxDuration": 300,
      "memory": 1024,
      "includeFiles": "node_modules/{@ffmpeg-installer,@ffprobe-installer}/**"
    },
    "api/chat.ts": { "maxDuration": 60 },
    "api/summarize.ts": { "maxDuration": 60 },
    "api/validate-key.ts": { "maxDuration": 60 },
    "api/audio/[sessionId].ts": { "maxDuration": 60 },
    "api/credits/purchase.ts": { "maxDuration": 60 },
    "api/stripe/create-checkout-session.ts": { "maxDuration": 60 },
    "api/transcribe-job/[jobId]/cancel.ts": { "maxDuration": 60 },
    "api/transcribe-job/[jobId]/status.ts": { "maxDuration": 60 },
    "api/user-settings/api-key.ts": { "maxDuration": 60 },
    "api/webhooks/stripe.ts": { "maxDuration": 60 }
  }
}
